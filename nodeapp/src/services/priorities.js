const db = require('../configs/database.config');

function professorPriority(classSchedule, data) {
    console.log(data.courses[2]);
}

async function putProfessors () {
    
}

module.exports = {
    professorPriority
}
